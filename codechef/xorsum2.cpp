#include<iostream>
#include<vector>
#include<algorithm>

using namespace std;

typedef long long ll;

#define f(i, n) for(int (i)=(0); (i)<(n); (i)++)
#define N 998244353
#define M 2e5+7

ll dp[M];
int n;
vector<int> a(M);

int bitsum(int a){
    bitset<32> b(a);
    return (int)b.count();
}

ll xorsum(int k){
    for (int i=0; i<exp(2, n) && ; i++)
}

int main(){
    cin>>n;
    f(i, n) cin>>a;
    cin>>q;
    while(q--){
        cin>>m;
        int ans =0;

    }
    return 0;
}
